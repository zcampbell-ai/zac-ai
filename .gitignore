
# macOS
.DS_Store

# Local backup copies
*.backup-*

# Environment files and credentials
.env
.env.*
!.env.example
!.env.*.example
/secrets/
/credentials/
*.pem
*.key
*.p12
*.pfx

# Private operational data
/data/
/private-data/
/exports/
/backups/

# D028 Lane B backup artifacts and key material (defense in depth - these
# should never be created inside the repo; private identities already
# use the .key extension, covered above)
*.age

# D030 raw ingestion artifacts (defense in depth - the configured
# artifact_store_root should never be inside the repo either, but this
# covers the default value)
/var/artifacts/
/logs/
*.log
*.sqlite
*.sqlite3
*.db
*.db-wal
*.db-shm
*.sqlite-wal
*.sqlite-shm
*.sqlite3-wal
*.sqlite3-shm

# Local development files
.venv/
venv/
__pycache__/
*.pyc
node_modules/

# Claude Code machine-specific settings
.claude/settings.local.json
